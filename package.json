{
  "name": "incoming-webhook",
  "version": "1.0.0",
  "description": "Incoming webhook for whatsapp integration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Detrans.io",
  "license": "ISC",
  "dependencies": {
    "mysql": "^2.18.1"
  }
}
